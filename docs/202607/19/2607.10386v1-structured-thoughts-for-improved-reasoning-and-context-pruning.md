---
title: Structured Thoughts For Improved Reasoning And Context Pruning
title_zh: 结构化思维：改进推理与上下文修剪
authors: "Zain Sarwar, Supriyo Chakraborty, Berkcan Kapusuzoglu, Chia-Hsuan Lee, Anirban Das, Stephen Rawls, Kartik Balasubramaniam, Sambit Sahu"
date: 2026-07-11
pdf: "https://arxiv.org/pdf/2607.10386v1"
tags: ["query:vlm-cot"]
score: 9.0
evidence: 直接针对大语言模型中的链式思考推理
tldr: "长链推理通常冗长且内存效率低。Structured Thoughts框架将推理组织为<try>和<outcome>交替块，并利用LLM总结每个步骤。微调后的模型在推理基准上性能提升高达8.08%，且可通过剪除<try>实现平均85%上下文节省，性能仅下降8.67%。该方法为提升LLM推理效率和内存使用提供了有效方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10386v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1411, \"height\": 465, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10386v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1444, \"height\": 367, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10386v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 504, \"height\": 507, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.10386v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1441, \"height\": 583, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10386v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1443, \"height\": 224, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10386v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1445, \"height\": 321, \"label\": \"Table\"}]"
motivation: 现有长链推理存在冗长和内存低效问题，需要结构化组织以提升效率。
method: "构建<try>和<outcome>交替的推理结构，通过LLM总结生成数据集，并微调基础模型。"
result: "在推理基准上性能提升高达8.08%，剪除<try>后平均节省85%上下文，性能下降8.67%。"
conclusion: 结构化推理结合上下文剪枝可有效提升LLM推理效率与内存使用。
---

## 摘要
大型语言模型（LLM）在生成长链思维方面表现出色，但冗长的推理轨迹往往啰嗦且内存效率低。在这项工作中，我们引入了结构化思维（Structured Thoughts）框架，该框架将推理组织成交替的<try>和<outcome>块：<try>捕获探索性的草稿工作，而<outcome>包含该步骤的提炼结论。我们通过将推理轨迹分割成<try>块并提示LLM将每个步骤总结为对应的<outcome>，构建了结构化思维数据集。在此重新格式化的数据上微调预训练基础模型，使模型采用结构化推理风格，与标准SFT相比，在推理基准测试上性能提升高达8.08%。显式结构还支持上下文修剪：在每个<try>/<outcome>对之后，可以修剪<try>，使模型在保留结论的同时无需在上下文中保留完整的草稿工作。概念验证的修剪实现在数学任务上平均节省85%的内存/上下文，同时性能下降8.67%。

## Abstract
Large language models (LLMs) excel at generating long chains of thought, but long reasoning traces are often verbose and memory-inefficient. In this work, we introduce Structured Thoughts, a framework that organizes reasoning into alternating <try> and <outcome> blocks: <try> captures exploratory scratch work, while <outcome> contains the distilled conclusion of that step. We construct a dataset of structured thoughts by segmenting reasoning traces into <try> blocks and prompting an LLM to summarize each step into its corresponding <outcome>. Fine-tuning pretrained foundation models on this reformatted data produces models that adopt the structured reasoning style, leading to performance gains of up to 8.08\% on reasoning benchmarks compared to standard SFT. The explicit structure also enables context pruning: after each <try>/<outcome> pair, the <try> can be pruned, allowing the model to retain conclusions without keeping the full scratch work in the context. A proof-of-concept pruning implementation achieves an average of 85\% memory / context savings with an 8.67\% performance drop across mathematical tasks.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

---

#### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：大型语言模型（LLM）虽然在多步推理任务中表现优异，但生成的长链推理轨迹往往冗长、重复，导致内存（KV-cache）和计算开销巨大，且“迷失在中间”现象表明模型难以有效利用长上下文的中间信息。
- **整体含义**：现有推理轨迹缺乏结构化组织，草稿（scratch work）与结论（conclusion）混杂，既降低了推理准确性，又浪费了上下文资源。论文提出**结构化思维（Structured Thoughts）** 框架，通过将推理步骤显式区分为探索性<ty>块和精炼性<outcome>块，同时提升推理性能和上下文效率，并利用这种结构实现上下文修剪（context pruning），在推理时丢弃草稿保留结论，显著节省存储和计算。

---

#### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：将每个推理步骤组织为一个交替出现的`<try>/<outcome>`对，其中`<try>`包含探索性草稿（如代数推导、验证），`<outcome>`包含该步的蒸馏结论（如中间结果或关键方程）。通过微调（SFT）使模型学会自动生成这种结构化轨迹；进一步利用结构信息，在训练时通过掩码机制模拟修剪，并在推理时实际删除已完成的`<try>`块，仅保留`<outcome>`上下文。
- **关键技术细节**：
    1. **数据集构建**：基于现有数学推理数据集（Llama-Nemotron Math v1.1）的原始轨迹，先通过启发式规则（检测“Wait”“Hmm”等决策词）分割成`<try>`块，再调用指令微调模型（Llama-70B-Instruct）为每个`<try>`生成一个简洁的`<outcome>`（1-2句话或方程）。数据集不改变原问题和答案，仅在轨迹中添加结构化格式。
    2. **结构化SFT**：对模型进行标准交叉熵训练，使其输出遵循`<try>/<outcome>`交替格式。
    3. **修剪感知训练（Masked SFT）**：在第二次SFT中，使用自定义注意力掩码（基于Flex Attention实现）。规则：当查询位置`q`位于`<outcome>`结束之后，键`k`落在对应的已完成`<try>`内时，`q`不能关注`k`。这迫使模型只依赖前面的`<outcome>`进行后续推理，模拟修剪下的信息流。
    4. **推理时修剪**：解码过程中，每生成一个完整的`<try>/<outcome>`对后，立即从KV-cache和上下文中移除该`<try>`块，保留`<outcome>`。位置编码（RoPE）索引不变，使用HuggingFace自定义循环实现。
- **无额外公式或算法流程**（均为文字描述）。

---

#### 3. 实验设计
- **数据集**：
    - **训练数据集**：Llama-Nemotron Math v1.1 的前100万条数学推理样本。结构化SFT版本基于同样的100万条，仅格式化为`<try>/<outcome>`结构。
    - **评估基准**：7个数学推理基准：MATH-500、MinervaMath、AMC23、AIME24、TheoremQA、OlympiadBench、GSM8K。
- **基准模型**：
    - Llama-Nemotron-8B（预训练推理模型）
    - Qwen2.5-7B-Instruct (s1)（经过s1K-1.1微调的版本）
- **对比方法**：
    - **Baseline SFT**：在原始未结构化轨迹上微调。
    - **Structured SFT**：在结构化轨迹上微调（本文方法）。
    - **Compute-Matched SFT（消融）**：将结构化数据中<outcome>块的内容替换为等长的`[MASK]`符号，保持序列长度和训练浮点运算量不变，以验证改进是否仅来自额外计算量。
    - **Structured SFT + Pruning**：推理时对结构化模型进行修剪（仅在Llama-Nemotron-8B上测试）。

---

#### 4. 资源与算力
- **明确说明**：实验在分布式**8节点**上运行，每个节点配备**8张 NVIDIA A100 40GB GPU**（共64张A100），节点间通过EFA连接。
- **训练框架**：veRL (Sheng et al., 2025) 和 LlamaFactory (Zheng et al., 2024)。
- **超参数**：学习率1×10⁻⁴，训练2个epoch，最大上下文长度25,000 tokens，采用序列打包（sequence packing）提高GPU利用率。
- **未明确训练时长**（如小时数），但给出以上配置。推理时修剪实验使用HuggingFace自定义循环，速度显著慢于优化引擎（如vLLM），导致额外开销。

---

#### 5. 实验数量与充分性
- **实验总量**：主要结果包含3组对比（Baseline SFT vs Structured SFT，两个模型，7个基准，共14个评测点）；修剪实验（Llama-Nemotron-8B，7个基准）；Compute-Matched消融（一个模型，7个基准）。总计约35个基准分数报告。
- **充分性与客观性**：
    - **充分**：覆盖了两个不同家族（Nemotron和Qwen）的7B级别模型，多个难度等级的数学基准；进行了关键消融（计算量匹配）验证结构化本身的价值；修剪实验给出了实际收益和代价（性能下降8.76% vs 85%压缩率）。
    - **不足**：修剪实验仅在一个模型（Llama-Nemotron-8B）上执行，缺少多模型验证；推理时修剪采用效率低的自定义实现，未在工业级引擎（如vLLM）上验证；未进行剪枝后的更长序列或多轮对话等场景测试；分割启发式和外部LLM总结的质量未量化评估。
    - **公平性**：对比时，结构化SFT与Baseline SFT使用相同训练样本数、迭代次数和超参数，计算量匹配消融也控制了序列长度和FLOPs，比较合理。

---

#### 6. 论文的主要结论与发现
- **结构化SFT提升推理准确性**：在Llama-Nemotron-8B上平均相对提升**+3.66%**，在Qwen2.5-7B-Instruct (s1)上平均提升**+8.08%**。所有基准中除个别外均有提升，尤其AIME24上Qwen模型提升60.06%（33.3→53.3）。
- **修剪可行但带来适度性能损失**：对于Llama-Nemotron-8B，修剪后平均性能下降**8.76%**，但节省约85%上下文内存（因为<outcome>平均长度只有<try>的15%）。这证明结构化修剪是有前景的概念验证。
- **结构化优势超越单纯增加计算量**：Compute-Matched消融显示，仅增加序列长度（用MASK填充）只能带来1.69%平均提升，远低于结构化SFT的3.66%，证实蒸馏的<outcome>提供了结构化信息增益。
- **长上下文本身不是万能药**：引用先前工作说明过长轨迹可能损害性能，印证结构化的必要性。

---

#### 7. 优点
- **方法创新**：首次将推理步骤显式分为草稿和结论，并利用该结构同时提升准确率和实现上下文修剪，思路简洁有效。
- **实验设计细致**：包含计算量匹配消融，有力证明了结构化格式本身的贡献，而非单纯的参数增多。
- **实际效率收益**：修剪后85%上下文节省极具吸引力，尤其适用于长推理场景（如竞赛数学、代码生成）。
- **通用性**：方法不依赖特定架构或优化器，可应用于多种基座模型，且公开代码和数据（计划开源）。

---

#### 8. 不足与局限
- **修剪实验结果不够全面**：仅在Llama-Nemotron-8B上测试，缺少在Qwen等模型上的验证，结论泛化性存疑。且推理时修剪实现效率低下（HuggingFace而非vLLM），部署性能未评估。
- **性能下降非完全可忽略**：8.76%的平均下降在追求高精度的任务中可能不可接受，需进一步优化（如作者建议引入RL阶段微调）。
- **数据构建依赖外部模型**：分割基于启发式，总结依赖Llama-70B-Instruct，可能引入噪音或遗漏关键逻辑。分割错误直接导致修剪后信息丢失。
- **缺少更广泛的场景验证**：仅测试数学推理，未涉及逻辑、编程、科学问答等领域；未测试长短文本混合、多轮对话等场景。
- **未与最新方法充分对比**：与PENCIL做了定性比较但无定量实验；未与过程奖励模型（如PRM）、树搜索等基线对比结构化SFT的优势。

---

（完）
