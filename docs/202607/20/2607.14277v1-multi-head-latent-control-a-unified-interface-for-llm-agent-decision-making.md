---
title: "Multi-Head Latent Control: A Unified Interface for LLM Agent Decision Making"
title_zh: 多头潜在控制：LLM智能体决策的统一接口
authors: "Amirhosein Ghasemabadi, Ruichen Chen, Bahador Rashidi, Di Niu"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.14277v1"
tags: ["query:vlm-cot"]
score: 8.0
evidence: 用于智能体推理决策的潜在控制接口，与链式推理相关
tldr: "大语言模型作为智能体时需要自主决策是否继续推理、延迟、调用工具等。现有方法依赖输入侧信号或外部编排，成本高且难以维护。本文提出Multi-Head Latent Control，从冻结LLM的隐藏状态轨迹中学习控制信号，实现能力预测和解决决策。该方法在路由系统中将大模型使用量降低多达90.7%，工具调用得分提升158%，缺失调用减少65.5%，显著改善质量-成本权衡。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.14277v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1437, \"height\": 536, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14277v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1419, \"height\": 550, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14277v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1319, \"height\": 889, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14277v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1435, \"height\": 538, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.14277v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1480, \"height\": 398, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14277v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1453, \"height\": 1003, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14277v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1452, \"height\": 292, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14277v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1226, \"height\": 278, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14277v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1432, \"height\": 341, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14277v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1453, \"height\": 340, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14277v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 923, \"height\": 293, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14277v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 875, \"height\": 212, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14277v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 464, \"height\": 213, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14277v1/table-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1438, \"height\": 233, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14277v1/table-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 907, \"height\": 254, \"label\": \"Table\"}]"
motivation: 现有LLM智能体决策依赖输入侧信号或外部编排，成本高且难以随模型更新维护。
method: 提出Multi-Head Latent Control，从冻结LLM的隐藏状态轨迹学习控制信号，包含能力头和解决头。
result: "在路由执行中大模型使用量降低最多90.7%，工具调用得分提升158%，缺失调用减少65.5%。"
conclusion: 为LLM智能体提供轻量、高效的控制接口，改善质量-成本权衡，支持后验适配。
---

## 摘要
大型语言模型越来越多地被部署为智能体，但可靠的智能体行为需要的不仅仅是下一个词预测。在推理时，智能体应能决定是否继续当前推理、将任务委托给更强模型、请求额外信息、调用外部工具或在给定设置下放弃。现有方法通过提示级路由、外部编排或任务特定微调来处理这些决策，这些方法主要依赖输入侧信号，且随着模型主干演变，通常成本高昂且难以维护。我们询问是否可以直接从模型的潜在生成过程中推断此类控制决策。我们引入了多头潜在控制，这是一个轻量级层，从冻结的LLM或VLM中读取隐藏状态轨迹，以生成部署时的控制信号。能力头预测当前模型能否解决实例，还是应委托给更强的协作者；而解决头预测适当的解决决策：澄清、工具使用、放弃或直接回答。两个头仅在来自同一冻结LLM主干的潜在轨迹上训练，从而能够在不修改模型的情况下进行事后适应。在语言和视觉-语言设置中，多头潜在控制持续改善多模型系统的质量-成本权衡，实现从部分生成的早期交接和更准确的干预决策。在路由执行（小模型+大模型）中，它在AndroidWorld上减少大模型使用高达90.7%，在基准测试中平均减少27-53%，同时保留大模型的大部分性能。此外，学习的控制信号提高了工具使用决策质量，相对得分提升高达158%，并减少了65.5%的遗漏必需工具调用。

## Abstract
Large language models are increasingly deployed as agents, but reliable agentic behavior requires more than next-token prediction. At inference time, it is preferred that an agent can decide whether to proceed with its current reasoning, defer to a stronger model, request additional information, invoke external tools, or abstain under the given setup. Existing approaches address these decisions through prompt-level routing, external orchestration, or task-specific fine-tuning, which primarily rely on input-side signals, and are often costly and difficult to maintain as model backbones evolve. We ask whether such control decisions can be inferred directly from a model's latent generation process. We introduce Multi-Head Latent Control, a lightweight layer that reads hidden-state trajectories from a frozen LLM or VLM to produce deployment-time control signals. A Capability Head predicts whether the current model can solve the instance or should defer to a stronger collaborator, while a Resolution Head predicts appropriate resolution decision Clarification, Tool Use, Abstention, or Direct Answering. Both heads are trained only on latent traces from the same frozen LLM backbone, enabling post hoc adaptation without modifying the model. Across language and vision-language settings, Multi-Head Latent Control consistently improves the quality-cost tradeoff of multi-model systems, enabling early handoff from partial generations and more accurate intervention decisions. In routed execution (small + large model), it reduces large-model usage by up to 90.7 percent on AndroidWorld and 27-53 percent on average across benchmarks, while retaining most of large-model performance. Additionally, the learned control signals improve tool-use decision quality, yielding up to +158 percent relative score gain and 65.5 percent fewer missed-required tool calls.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：大型语言模型（LLM）作为智能体在部署时，需要自主做出多种决策——是否继续当前推理、将任务委托给更强的模型、请求额外信息、调用外部工具，或在当前设置下放弃。现有方法（如提示级路由、外部编排、任务特定微调）主要依赖输入侧信号（如提示特征、预测的任务类别），成本高、难以随模型主干快速演化维护。
- **研究动机**：能否直接从模型自身的**潜在生成过程**（即隐藏状态轨迹）中推断出这些控制决策？这样可以在不修改或不微调主干模型的情况下，实现轻量、高效的部署时控制。
- **整体含义**：本文证明冻结的LLM/VLM可以暴露一个实用的控制接口——通过训练轻量级的“头”读取隐藏状态，预测模型是否足够胜任当前实例（能力预测）以及应该采取哪种解决行动（解决决策），从而改善多模型系统的质量-成本权衡，并支持更精准的工具调用等干预行为。

## 2. 论文提出的方法论

### 核心思想
- 提出 **Multi-Head Latent Control**（多头潜在控制），由两个轻量级头组成：
  - **Capability Head（能力头）**：预测当前模型是否足以解决当前实例，若不足则委托给更强的备份模型（m2）。
  - **Resolution Head（解决头）**：当决定留在当前模型时，预测合适的解决动作：请求信息（info）、调用工具（tool）、放弃（cant）或直接回答（direct answer）。
- 两个头从冻结主干的隐藏状态轨迹中读取信号，仅训练头，不修改主干。

### 关键技术细节
1. **处理流程**：
   - 输入x，主干m1生成输出ŷ = (ŷ₁,...,ŷ_N)。
   - 收集每一层的隐藏状态序列H^(ℓ) = [h₁^(ℓ); ...; h_N^(ℓ)] ∈ ℝ^{N×d}。
   - 能力头默认使用**最后一层**轨迹H_cap = H^(L)；解决头默认使用**中间层**轨迹H_res = H^(ℓ_res)。
   - 将变长轨迹压缩为固定长度表示：$\tilde{H}_{cap} = \Pi_{cap}(H_{cap})$, $\tilde{H}_{res} = \Pi_{res}(H_{res})$。

2. **预测输出**：
   - 能力头输出标量能力分数 $p_{cap} = \sigma(h_{cap}(z_{cap})) \in [0,1]$，其中 $z_{cap} = e_{cap} \circ \phi(\tilde{H}_{cap})$。
   - 解决头输出动作分数向量 $s_{res} = [s_{info}, s_{tool}, s_{cant}] \in [0,1]^3$，$\max_a s_{res,a} > \tau_{res}$ 时执行对应动作，否则直接回答。

3. **推理时策略**：
   - 先评估能力头：若 $p_{cap} < \tau_{cap}$（默认0.8），则委托给更强模型m2；否则留在m1。
   - 停留在m1时，评估解决头：若最高分数超过阈值（默认0.5），则执行对应干预；否则直接回答。

4. **训练**：
   - 主干冻结，只训练两个轻量级头。
   - **能力头**：训练数据由冻结主干生成输出，并用LLM裁判（Qwen3vl-30B-A3B）比较输出与参考答案得到标量得分（0~1）作为监督目标，使用加权均方误差（MSE）损失。
   - **解决头**：基于When2Call数据集，用外部裁判生成每个实例的正确解决标签，冻结主干生成隐藏轨迹，训练头预测标签，使用二分类交叉熵（BCE）损失。
   - 能力头训练数据为12万样本的混合数据（覆盖多种任务类型），解决头使用When2Call数据集。

## 3. 实验设计

### 使用的数据集/场景
- **多模型协作（路由执行）**：SimpleVQA（多模态事实性）、ScreenSpot-Pro（GUI grounding）、CharXiv-Reasoning（图表推理）、MathVerse（数学视觉）、MathVista（数学推理）、MMLU-Pro（广泛知识）、AndroidWorld（长时多步智能体环境）。
- **解决决策**：When2Call（结构化干预决策）。
- **工具调用**：TriviaQA（开放域问答，需决定是否使用网页搜索）。
- **前缀时间预测**：在上述基准上，仅用前200个生成token评估能力信号质量。

### Benchmark与对比方法
- **基准方法**：
  - 单小模型（m1）和单大模型（m2）作为上/下界。
  - 提示级自切换（prompt-level self-switching）：小模型被指示不确定时拒绝回答，然后转交大模型。
  - 模型token置信度（mean token probability）作为能力代理。
  - When2Call-SFT：专门微调的LLM作为比较。
- **主干模型**：Qwen3-VL（2B/4B/32B，含思考模式）、Qwen3.5（4B/9B/27B）、Gemma（4B/31B思考模式），涵盖不同家族、规模和推理模式。

### 实验数量与充分性
- **大量实验**：
  - 路由执行：6大模型家族×6个基准+AndroidWorld特殊案例（表1-2），含不同阈值分析。
  - Resolution控制：6种主干对比（表3）。
  - 工具决策：5种主干对比（表4）。
  - 前缀时间预测：3种主干×3个训练/推理设置（表5），带端到端路由效果（附录表6）。
  - 消融实验：
    - 能力头层选择（附录C.1）
    - 解决头层选择（附录C.2）
    - 训练数据广度（视觉-数学vs混合数据，附录C.3）
    - 前缀长度（50/200/1000/全长，附录C.4）
    - 对比自切换基线（附录B.2）和置信度基线（附录B.3）。
- **充分性评判**：实验覆盖多模型家族、多任务格式、多模态、长时间任务和结构化干预，对比了多种基线，消融设计合理，充分支持主要结论。

## 4. 资源与算力

- 文中明确说明：在9B规模的Qwen设置下，数据生成和训练在**单张高端GPU上一天内完成**；头训练本身在**16 GB GPU内存内**可以运行。
- **未明确说明的具体GPU型号**（如A100、H100等），也未给出完整训练时长统计（如各实验总GPU小时数）。因此对于算力细节描述部分有限。

## 5. 主要结论与发现

1. **路由执行**：能力头有效改善了质量-成本权衡。例如：
   - AndroidWorld上：Qwen3-VL-4B→32B，成功分数从0.47提升至0.60，付费API成本降低**90.7%**；Qwen3.5-9B→27B，分数从0.51提升至0.56，成本降低**85.8%**。
   - 全基准平均：路由系统降低大模型使用27-53%，同时保留大部分性能。
2. **解决决策**：解决头显著优于主干的原始表现。例如当2Call上，Qwen-VL-2B的F1从37.3提升至49.0，准确率从52.7提升至65.1。
3. **工具调用决策**：能力头改善TriviaQA上的web搜索决策，相对得分提升最高158%，遗漏必需web调用减少65.5%。
4. **前缀时间能力预测**：可以通过前缀轨迹（如200 token）进行早期能力评估，虽弱于全长轨迹，但比直接应用全长训练头更好。训练在相同前缀长度下效果更佳。
5. **消融**：能力头的最好层是最终层；解决头最好层是中间层；训练数据广度至关重要——仅视觉-数学数据在ScreenSpot Pro上转移差（ROC-AUC 0.62 vs 混合0.84）。

## 6. 优点

| 方面 | 亮点 |
|------|------|
| **方法轻量** | 不修改主干，仅训练两个小头，可在单GPU上一天内完成，内存需求低（<16GB）。 |
| **统一接口** | 能力头和解决头覆盖多种部署时决策（模型选择、干预选择），无需外部编排模型。 |
| **后验适应** | 可快速附着到新主干模型上，适应模型演化而不需重新训练整个模型。 |
| **实验广泛** | 覆盖多模型家族（Qwen3-VL, Qwen3.5, Gemma）、多模态、多任务类型（视觉、文本、工具、长时间）以及多个部署变体（路由、早停、工具决策）。 |
| **公平对比** | 对比了多种相关基线（自切换、置信度、微调模型），消融充分。 |
| **显著结果** | 成本降低幅度大（最高90.7%），性能损失小；工具决策质量提升明显。 |

## 7. 不足与局限

- **控制信号质量直接约束效率**：在长时任务中，微小误差可能累积，影响整体系统性能。作者也指出未来需要进一步提高鲁棒性和校准。
- **未探索更多控制头**：论文仅研究了能力和解决两个具体头，但潜在可用的控制信号（如安全过滤、置信度塑形等）未实现。作者承认这是未来方向。
- **算力报告不完整**：未提供具体GPU型号和总训练时间（如GPU小时数），对于完全复现或成本评估有一定障碍。
- **解决头仅在一个基准（When2Call）上评估**：虽然该基准覆盖多种干预类型，但缺少在其他场景（如更复杂的工具调用环境）的验证。
- **训练数据依赖外部裁判**：能力头和解决头需要LLM裁判生成监督标签，可能引入裁判偏差且增加前期成本。
- **路由策略简单**：使用固定阈值，未自适应调整。更细粒度的动态阈值策略可能进一步改善权衡。
- **提示级自切换基线较弱**：虽然对比结果充分证明了潜在控制的优势，但自切换基线可能不是最优的提示设计，未来可考虑更高级的提示策略（如思维链自反思）。

## 8. 总结

论文通过提出多头潜在控制，成功将部署时控制决策基于隐藏状态轨迹，为冻结LLM提供了轻量、高效的控制接口。实验表明该方法能显著改善质量-成本权衡，且易于后验适配新模型。这是一项扎实、系统的研究，对未来LLM智能体部署具有实际指导意义。

（完）
